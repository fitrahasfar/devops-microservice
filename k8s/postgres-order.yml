apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-order
  namespace: devops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-order
  template:
    metadata:
      labels:
        app: postgres-order
    spec:
      containers:
        - name: postgres
          image: postgres:15
          imagePullPolicy: IfNotPresent
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              value: orderdb
          ports:
            - containerPort: 5432

---
apiVersion: v1
kind: Service
metadata:
  name: postgres-order
  namespace: devops
spec:
  selector:
    app: postgres-order
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
